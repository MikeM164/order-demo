version: 0.2
phases:
  build:
    commands:
      - echo Entered build phase ...
      - mvn clean package
  post_build:
    commands:
      - echo Build completed successfully
artifacts:
  base-directory: .
  files:
    - target/order-demo.jar
    - scripts/***
    - appspec.yml
  discard-paths: yes
